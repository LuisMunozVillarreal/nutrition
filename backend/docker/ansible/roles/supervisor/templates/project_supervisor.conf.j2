[program:django]
command=gunicorn config.wsgi -c /etc/gunicorn.conf.py
{% include "cmd_vars.j2" %}

[program:nginx]
command=/usr/sbin/nginx -g 'daemon off;'
autorestart=true

[group:{{ project_name }}]
programs=django,nginx
