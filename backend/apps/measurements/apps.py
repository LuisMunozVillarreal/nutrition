"""measurements app config module."""

from django.apps import AppConfig


class MeasurementsConfig(AppConfig):
    """MeasurmentsConfig class."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.measurements"
