[MASTER]

# List of plugins (as comma separated values of python modules names) to load,
# usually to register additional checkers.
load-plugins=pylint.extensions.docparams,pylint_django,pylint.extensions.docstyle

good-names=
    db, rf

# Parallel jobs
jobs=0

# Reports
reports=y

max-line-length=79

function-rgx=[a-z_][a-z0-9_]{2,50}$

django-settings-module=config.settings


[MESSAGES CONTROL]

disable=
    unused-argument,
    fixme,
    redefined-outer-name,
    C0103,
    duplicate-code,


[SIMILARITIES]
ignore-imports=yes
